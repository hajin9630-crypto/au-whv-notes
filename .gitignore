# 로컬 미리보기 서버만 제외 (.claude/skills 는 저장소에 포함)
.claude/serve.js
.claude/launch.json

# OS
Thumbs.db
desktop.ini
.DS_Store

# 개인 문서 — 실수로 이 폴더에 넣더라도 커밋되지 않게
*.pdf
*.docx

# 자료를 뽑으려고 잠깐 떨궈두는 폴더들.
# 한 번 실수로 통째로 커밋된 적이 있어 미리 막아둔다.
새 폴더/
새 폴더*/
새폴더*/
temp/
tmp/
_raw/
